# Development dependencies. Not needed to run corparius.
#   pip install -r requirements-dev.txt
#
# Tests alone live in requirements-test.txt, which the CI matrix installs. This
# file adds PyInstaller on top: it trails new CPython releases by months, so
# keeping it here stops one missing wheel from taking out every leg of a matrix
# whose tests never touch it.
-r requirements-test.txt

# Builds the standalone binaries (see packaging/corparius.spec).
pyinstaller>=6.0
