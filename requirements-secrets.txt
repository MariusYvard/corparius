# Optional: encrypt secrets at rest. Only needed when CORP_SECRET_KEY is set.
#   pip install -r requirements-secrets.txt
# See docs/securite.md. Without this, corparius runs fine with secrets in the
# clear (the default), which the doctor flags.
cryptography>=42
