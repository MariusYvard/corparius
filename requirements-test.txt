# Test and quality dependencies, split out from requirements-dev.txt so the test
# matrix does not drag in PyInstaller. PyInstaller trails new CPython releases by
# months, so a single missing wheel would take out every leg of the matrix rather
# than just the packaging job.
#   pip install -r requirements-test.txt
-r requirements.txt
pytest>=8.0
coverage>=7.4

# Optional at-rest encryption (runtime-optional), installed here so its tests
# actually run rather than skipping quietly. See requirements-secrets.txt.
cryptography>=42

# Lint and types. Configured in pyproject.toml; run them with
#   ruff check .
#   mypy app/models.py app/paths.py app/settings_spec.py
# mypy is deliberately scoped to a ratchet list rather than all of app/ - see
# the comment above [[tool.mypy.overrides]] in pyproject.toml.
ruff>=0.6
mypy>=1.11
