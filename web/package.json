{
  "name": "corparius-console",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "The operator console. Built in CI; the wheel and the frozen binary serve the output with no Node installed.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "check": "svelte-check --tsconfig ./jsconfig.json"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^6.2.1",
    "svelte": "^5.43.11",
    "vite": "^7.1.14"
  },
  "engines": {
    "node": "^20.19 || ^22.12 || >=24"
  }
}
